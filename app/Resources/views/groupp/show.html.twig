{% extends 'base.html.twig' %}

{% block body %}
    <h1>Groupp</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupp.id }}</td>
            </tr>
            <tr>
                <th>Groupp</th>
                <td>{{ groupp.groupp }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ groupp.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('groupp_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('groupp_edit', { 'id': groupp.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
